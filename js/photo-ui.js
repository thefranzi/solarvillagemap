/* photo-ui.js: safe stub (no-op unless #photo-capture exists) */
document.addEventListener('DOMContentLoaded', function(){
  if(!document.getElementById('photo-capture')) return;
  // TODO: implement camera workflow here if needed.
});
